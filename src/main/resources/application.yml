spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/controlefinanceiro-dev
    username: postgres
    password: 123
    platform: postgres
    driverClassName: org.postgresql.Driver

server:
  servlet:
    context-path: /controle-financeiro-local

application:
  security:
    jwt:
      secret-key: 586E3272357538782F413F4428472B4B6250655367566B597033733676397924
      expiration: 10000000
      refresh-token:
        expiration: 1000000

security:
  host: http://localhost:4200